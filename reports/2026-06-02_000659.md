# Report 2026-06-02 00:06 MSK

## What was fixed
- **Cargo.lock** — убран из .gitignore для воспроизводимых Docker-сборок
- **Vosk SDK в CI** — добавлен `sudo` перед `cp` и `ldconfig` (Permission denied на GitHub runner)
- **clippy в event.rs** — 3 `if` внутри match коллапсированы в match-guard (KeyCode::Up, PageUp, Home)
- **fmt** — `cargo fmt` поправил порядок импортов в main.rs

## Problems encountered
- `check (all engines)` падал: `cp: cannot create regular file '/usr/local/lib/libvosk.so': Permission denied`
- clippy с `tui` выводил 3 ошибки `this if can be collapsed into the outer match`
- `cargo fmt --check` в CI падал — форматирование не было применено после изменений main.rs

## How they were solved
- Vosk SDK — добавлен `sudo` в ci.yml
- event.rs — заменены `if`-внутри-arm на match-guard: `KeyCode::Up if !app.segments.is_empty()`
- fmt — прогон `cargo fmt` и коммит результата
