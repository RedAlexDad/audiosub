# Report 2026-08-28_085527 MSK

## What was added
- Раздел «GPU (CUDA)» в `AGENTS.md` и `docs/build.md`: включение фичи
  `cuda` у зависимости `whisper-rs`, сборка с `CUDACXX`/`CUDA_PATH`,
  ограничение драйвера (потолок CUDA 13.2), CPU-fallback, правило
  `curl -4 --http1.1`.
- Пункт «GPU-ускорение Whisper» в README, пометка `whisper:large` = large-v3.
- В `.gitignore` добавлено `ggml-*.bin` — скачанные модели в корне репо
  больше не рискуют попасть в коммит (проверено на `ggml-large-v3.bin`, 3 ГБ).

## What was changed
- `AGENTS.md`: новый раздел `GPU (CUDA)`; обновлён блок про модели
  (`whisper:large` → `ggml-large-v3.bin`, скачивание в текущую директорию).
- `README.md`: пункт про CUDA-ускорение; `--download-model whisper:large`
  помечен как large-v3 со ссылкой на docs/models.md.
- `docs/build.md`: новый раздел `GPU (CUDA)` с примером `Cargo.toml`
  и командой сборки.
- `docs/models.md`: `whisper:large` → `ggml-large-v3.bin`, примечание про
  удалённый v1; curl-команды с `-4 --http1.1`; в таблицу моделей добавлен
  столбец «Файл».

## Problems encountered
- В рабочей директории лежал незакоммиченный `ggml-large-v3.bin` (3 ГБ) —
  `.gitignore` не покрывал бинарники моделей в корне, только `models/`.

## How they were solved
- В `.gitignore` добавлена маска `ggml-*.bin`; файл остался локально,
  в индекс не попал.