# Report 2026-06-02 02:55 MSK

## What was changed
- **`.cargo/config.toml`** — добавлен `[env]` с `WHISPER_DONT_GENERATE_BINDINGS=1`
- **`mk/quality.mk`** — `WHISPER_DONT_GENERATE_BINDINGS=1` добавлен в test/check/lint/verify цели
- **`src/asr/vosk_backend.rs`** — `partial` парсится из JSON, а не используется сырой ответ Vosk
- **`src/asr/vosk_dl.rs`** — `unsafe impl Send/Sync` для VoskDl, Model, Recognizer

## What was fixed
- Vosk partial больше не показывает `{"partial":"..."}` в TUI
- bindgen краш при `make verify` — env var теперь в cargo config + Makefile
- `run_tui` слишком много аргументов — добавлен `#[allow(clippy::too_many_arguments)]`

## Problems encountered
- Vosk возвращает raw JSON, нужен `serde_json::from_str` для извлечения partial
- whisper-rs-sys требует `WHISPER_DONT_GENERATE_BINDINGS=1` при сборке на системе пользователя
- `NonNull<c_void>` не реализует `Send` — пришлось добавить unsafe impl
