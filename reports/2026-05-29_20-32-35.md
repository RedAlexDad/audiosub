# Report 2026-05-29 20:32:35 UTC

## What was added
- Makefile цели `build-both`, `run-both`, `release-both` — сборка с обоими ASR-движками (vosk + whisper) в одном бинарнике
- Разделы в help (`## Сборка ##`, `## Запуск ##`, `## Модели ##`, и т.д.) с разделителями `---`

## What was changed
- `Makefile`: help переведён на русский, добавлены `build-both`/`run-both`/`release-both`
- `audiosub.toml`: engine переключён с whisper на vosk по умолчанию
- `reports/decomposition_plan.md`: Phase 2 (Whisper.cpp) помечена завершённой — 3.4 и 3.6 `[x]`

## Problems encountered
- `audiosub.toml` был настроен на whisper, но `make run` (дефолтные фичи) использует vosk — несоответствие

## How they were solved
- Конфиг приведён в соответствие: `engine = "vosk"`, `model_path = "models/vosk-model-small-ru-0.22"`
- Добавлена цель `build-both` для бинарника с обоими движками — переключение через audiosub.toml без пересборки
