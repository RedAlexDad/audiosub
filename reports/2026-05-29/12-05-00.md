# Отчёт 2026-05-29 12:05 UTC

## What was changed
- `tui/mod.rs`: после истечения ticks у `message` теперь очищается `self.message = None`

## Problems encountered
- Баг: после нажатия `s` сообщение "Saved: ..." зависало навсегда, блокируя показ распознанного текста и повторный экспорт

## How they were solved
- При `*ticks == 0` после декремента устанавливаем `self.message = None`
