# Отчёт 2026-05-29 12:02 UTC

## What was added
- Экспорт субтитров горячей клавишей: `s` → SRT, `S` (Shift+S) → TXT
- Файлы сохраняются как `audiosub_YYYY-MM-DD_HH-MM-SS.{srt,txt}` в текущую директорию
- Уведомление "Saved: ..." в панели Recognition (зелёным, 15 кадров)

## What was changed
- `tui/mod.rs`: добавлены `message: Option<(String, u8)>`, `export_srt()`, `export_txt()`
- `render_middle` показывает сообщение вместо partial, зелёным цветом
- `render_top`: хинт обновлён до `q/p/s/S/↑↓`
- `handle_input`: обработка `s` (SRT) и `S` (TXT)
- Импорты: `chrono::Local`, `std::fs::File`, `std::io::Write`

## Problems encountered
- Clippy: collapsible if

## How they were solved
- Слил два if в один через `&&`
