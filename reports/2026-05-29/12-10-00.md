# Отчёт 2026-05-29 12:10 UTC

## What was changed
- `tui/mod.rs`: `export_srt()` и `export_txt()` теперь сохраняют файлы в `saved/audiosub_*.{srt,txt}`
- Создание директории `saved/` через `fs::create_dir_all` при первом экспорте

## Problems encountered
- нет
