# Report 2026-06-01 23:40 MSK

## What was added
- `.github/workflows/release.yml` — GitHub Release workflow (сборка Linux binary, Docker → ghcr.io, создание Release по тегу v*)
- `.github/workflows/audit.yml` — еженедельный security audit (cargo audit + cargo outdated)
- `rust-toolchain.toml` — фиксация stable toolchain с компонентами rustfmt и clippy

## What was changed
- `.github/workflows/ci.yml` — полная переработка: матрица clippy/check (4 конфигурации: minimal, default, whisper+tui, all engines), cargo audit, cargo machete, Docker build с GHA cache, concurrency groups
- `scripts/ci-check.sh` — обновлён под новую матрицу CI (fmt, clippy×3, check×3, test)
- `src/cli.rs`, `src/session/mod.rs` — автоматическое форматирование cargo fmt

## Problems encountered
- cargo fmt проверка в CI упала из-за длинных строк, не укладывающихся в max_width=120
- CI не мог собрать с Vosk без libvosk.so — в чистом окружении его нет

## How they were solved
- Запущен `cargo fmt` для приведения кода к стандарту проекта
- В CI добавлен шаг загрузки Vosk SDK для джоб, где включён vosk (условно через `contains(matrix.args, 'vosk')`)
