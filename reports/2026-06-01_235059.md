# Report 2026-06-01 23:50 MSK

## What was changed
- `src/session/mod.rs:121` — `sample_rate` unused в `--no-default-features`, добавлен `#[allow(unused_variables)]`
- `src/main.rs` — cfg-gated импорты `PathBuf`, `AudioCapture`, `SubtitleBuffer`, `SubtitleOutput` под `feature = "tui"`; перенесён `max_duration` в cfg-gated блок
- `Cargo.toml` — добавлен `[package.metadata.cargo-machete]` с ignored-списком ложных срабатываний
- `ci.yml`, `release.yml`, `audit.yml` — обновлены: в CI добавлен `WHISPER_DONT_GENERATE_BINDINGS=1` для whisper-сборок, Vosk SDK ставится условно

## Problems encountered
- `unused variable: sample_rate` в `create_engine()` при сборке без Vosk/Whisper — параметр нужен только когда один из движков включен
- `cargo machete` выдал 6 ложных срабатываний: `duct`, `libc`, `reqwest`, `thiserror`, `tokio`, `tracing-appender`
- `unused imports` и `unused variable: max_duration` в `main.rs` при `--no-default-features` — всё используется только под `tui`
- whisper-clippy падает с `WHISPER_DONT_GENERATE_BINDINGS=1` без переменной окружения

## How they were solved
- `sample_rate` — добавлен `#[allow(unused_variables)]` на функцию
- Ложные срабатывания machete — добавлены в `ignored` в Cargo.toml
- Импорты main.rs — cfg-gated под `feature = "tui"`, `max_duration` тоже cfg-gated
- В ci.yml добавлена env `WHISPER_DONT_GENERATE_BINDINGS=1` для whisper-джоб
