# Commit Report

**Date:** 2026-05-28 23:30:00 UTC
**Branch:** main
**Files changed:** Cargo.toml Makefile Dockerfile docker-compose.yml rustfmt.toml .gitignore .github/workflows/ci.yml .githooks/pre-commit .env.example AGENTS.md src/main.rs src/lib.rs src/cli.rs src/config.rs src/logging.rs src/audio/mod.rs src/audio/pulse.rs src/asr/mod.rs src/asr/vosk_backend.rs src/asr/whisper_backend.rs src/subtitle/mod.rs src/subtitle/srt.rs src/subtitle/vtt.rs src/tui/mod.rs

## What was added

- Cargo-проект с полной модульной структурой (cli, config, audio, asr, subtitle, tui, logging)
- CLI-аргументы через clap (--engine, --model, --output, --lang, --device, --format, --tui, -v)
- Система конфигурации через TOML + serde с дефолтными значениями
- Трейты AudioCapture / AsrEngine / SubtitleWriter для абстракции бэкендов
- Заглушки PulseAudio, Vosk, Whisper.cpp, SRT/VTT writer, TUI
- Makefile с целями build / test / run / lint / clean / docker / report
- Dockerfile (мультистейдж: builder + runtime)
- docker-compose.yml с сервисами app + dev
- GitHub Actions CI (check, clippy, fmt, test, docker build)
- .githooks/pre-commit — автоматическое создание отчёта перед коммитом
- AGENTS.md с правилами коммитов на русском
- rustfmt.toml, .env.example, .gitignore

## What was changed

- Обновлён reports/decomposition_plan.md: добавлены Docker, Compose, Makefile
- Исправлены версии зависимостей: rubato 0.17 → 3, vosk 0.4 → 0.3
- Добавлены impl Default для всех структур с new()
- Расставлены #![allow(dead_code)] для skeleton-кода

## Problems encountered

1. rubato 0.17 не существует — crate уже на версии 3.0.0
2. vosk 0.4 не существует — последняя версия 0.3.1
3. whisper-rs-sys требует multilib (gcc-multilib) для сборки — не критично, фича опциональна
4. Clippy ругался на new() без Default — добавлены impl Default
5. Форматирование не проходило check после правок — исправлено cargo fmt

## How they were solved

1-2. Версии в Cargo.toml скорректированы под актуальные
3. Feature `whisper` помечена как опциональная, по умолчанию не собирается
4. Добавлены derive/impl Default для всех структур
5. Запущен `cargo fmt`

---

