Commit Report

**Date:** 2026-05-28 20:36:31 UTC
**Branch:** main
**Files changed:** .github/workflows/ci.yml src/cli.rs reports/decomposition_plan.md

## What was added

- `.github/workflows/ci.yml`: три джобы — check (cargo check + clippy + fmt), test, docker
- Чек-лист статусов (`[x]`/`[ ]`) во все таблицы decomposition_plan.md
- Env-переменные `AUDIOSUB_CONFIG`, `AUDIOSUB_ENGINE`, `AUDIOSUB_MODEL`, `AUDIOSUB_OUTPUT`, `AUDIOSUB_LANG`, `AUDIOSUB_DEVICE`, `AUDIOSUB_FORMAT`, `AUDIOSUB_VERBOSE` в clap аргументы

## What was changed

- CI переведён с `--all-features` на `--no-default-features` (libvosk.so недоступен в GitHub Actions)
- `default_value` убран у `--output`, т.к. теперь `Option<PathBuf>` с env

## Problems encountered

- Старый CI был на `--all-features`, что падало бы без libvosk.so
- `default_value` конфликтовал с `Option` при добавлении env

## How they were solved

- CI теперь билдит без Vosk; Vosk-тесты будут добавлены в отдельный workflow при появлении runner с библиотекой
- Убран `default_value`, оставлен `Option` — clap корректно подхватывает env
