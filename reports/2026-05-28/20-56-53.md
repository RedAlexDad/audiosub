# Commit Report

**Date:** 2026-05-28 20:56:53 UTC
**Branch:** main
**Files changed:** Cargo.toml reports/2026-05-28_23-56-41.md reports/decomposition_plan.md src/audio/mod.rs src/cli.rs src/lib.rs src/main.rs src/tui/mod.rs 

## What was added

- CLI аргументы `--list-devices` и `--duration`
- TUI (ratatui + crossterm) с тремя панелями: статус, live-распознавание, сегменты
- Feature `tui` в Cargo.toml (включена в default)

## What was changed

- `main.rs`: рефакторинг — `run_session()` вынесен из main(); TUI-путь под `#[cfg(feature = "tui")]`
- `audio/mod.rs`: реэкспорт `list_sources`
- `lib.rs`: `mod tui` под cfg-гейтом
- `reports/decomposition_plan.md`: 5.1 и 5.2 помечены как `[x]`
- `src/tui/mod.rs`: заглушка заменена на полноценную реализацию TuiApp

## Problems encountered

- `lib.rs` включал `pub mod tui` без cfg-гейта — сборка падала без `tui`-feature
- `Box<dyn AsrEngine>` не приводится к `&mut dyn AsrEngine` напрямую
- `buffer.push(seg)` ждёт `Segment`, а не `&Segment`
- `CrosstermBackend` и `Terminal` не были импортированы в сигнатуре метода

## How they were solved

- Добавлен `#[cfg(feature = "tui")]` к `mod tui` в `lib.rs`
- Использован `engine.as_mut()` вместо `&mut engine`
- Исправлено на `seg.clone()`
- Добавлены `use ratatui::Terminal` и `use ratatui::prelude::CrosstermBackend`

---
