# Report 2026-06-02 00:25 MSK

## What was changed
- `.github/workflows/ci.yml` — добавлен `load: true` в docker/build-push-action

## Problems encountered
- Docker build не загружал образ в локальный daemon (`load: false` по умолчанию), из-за чего `docker run` падал с `pull access denied`

## How they were solved
- Явно указан `load: true` в параметрах build-push-action
